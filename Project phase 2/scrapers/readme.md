for all the scraping files. 
**To run Quiz_Scrapper.py.**
  -Install dependencies once: pip install requests beautifulsoup4 pandas.
  -Make sure you’re in the project root (Study_Buddy_hub/Project phase 2).
  -Run the scraper: python scrappers/Quiz_Scrapper.py.
  -The script fetches the GeeksforGeeks quiz, prints the dataframe to the console, and saves the raw results to data/Raw_data/quiz_data.csv (folder is created automatically).

  
